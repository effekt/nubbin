/** Before the preview panel first appears — long enough that travel across rows opens
 * nothing. */
export const PREVIEW_SHOW_DELAY_MS = 300;

/** Before an open preview panel closes — the grace that keeps row-to-row travel from
 * flickering. */
export const PREVIEW_HIDE_DELAY_MS = 150;
