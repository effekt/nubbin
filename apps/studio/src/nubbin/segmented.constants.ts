/* The wireframes' rule, and the specimen's: an enum an author can see whole lays flat as
 * segments; past this many members it folds into a dropdown. One home for the number, so
 * a top-level enum and one nested in a row fold at the same size. */
export const SEGMENTED_MAX_MEMBERS = 3;
