/** Why the add control is disabled, in the wireframes' words: the bound, stated as what
 * the list holds rather than what the author did wrong. */
export function holdsAtMostLine(max: number): string {
  return `This list holds at most ${max}.`;
}
