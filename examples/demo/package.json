{
  "name": "demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Reference marketing site whose components are shaped as Nubbin blocks.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run --project unit",
    "guardrail": "vitest run --project guardrail",
    "e2e": "vitest run --project e2e",
    "typecheck": "tsc --noEmit",
    "nubbin": "node ../../packages/cli/dist/bin.js",
    "fixtures:publish": "pnpm nubbin publish / && pnpm nubbin publish /dispatches && pnpm nubbin publish /dispatches/tide-tables && pnpm nubbin publish /live",
    "live:record": "pnpm nubbin publish / --config nubbin.live.config.ts && pnpm nubbin publish /dispatches --config nubbin.live.config.ts && pnpm nubbin publish /dispatches/tide-tables --config nubbin.live.config.ts && pnpm nubbin publish /live --config nubbin.live.config.ts",
    "serve:verify": "node scripts/verifyServing.mjs"
  },
  "dependencies": {
    "@nubbin/core": "workspace:*",
    "@nubbin/next": "workspace:*",
    "@nubbin/react": "workspace:*",
    "@nubbin/store-fs": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@nubbin/cli": "workspace:*",
    "@tailwindcss/postcss": "catalog:",
    "@testing-library/dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "happy-dom": "catalog:",
    "tailwindcss": "catalog:",
    "tsx": "4.23.12",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
