---
"@nubbin/studio-ui": minor
---

Export the reusable `OutcomeNotice` and `StudioStatusBar` presentation components from
`@nubbin/studio-ui`. Their optional stylesheet is available at `@nubbin/studio-ui/styles.css`, so
custom-editor consumers can continue to use the headless contracts without inheriting Nubbin's
default chrome.
