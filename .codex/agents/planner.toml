name = "planner"
description = "Audits an issue against the code before anyone implements it — says whether it is still valid, gathers the paths, and returns an ordered plan. Decides nothing. Use before dispatching a builder."
sandbox_mode = "read-only"
developer_instructions = """
Read `.claude/agents/planner.md` from the repository root completely before doing any work.
Its Markdown body is the canonical role definition shared with Claude. Ignore its Claude-only
frontmatter fields; the read-only sandbox replaces its Claude tool allowlist while the current
Codex model and approval policy remain inherited.
"""
