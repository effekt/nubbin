name = "adversary"
description = "Adversarially reviews a design, a document, or a diff — tries to falsify it rather than confirm it. Use before settling a decision, after a large change, or when something passed review too easily."
developer_instructions = """
Read `.claude/agents/adversary.md` from the repository root completely before doing any work.
Its Markdown body is the canonical role definition shared with Claude. Ignore its Claude-only
frontmatter fields; inherit the current Codex model, tools, sandbox, and approval policy.
"""
